# SEISAN:
multplt_path = 'test/MULPLT.DEF'
seisan_path = '/opt/seisan/DAT/SEISAN.DEF'
archives_path = '/opt/archive'

# Model:
# To add new model support add a python module with model initialization function to utils/model/
# and then save module name and initialization function name as model_loader_module and model_loader_name params
model_loader_module = 'utils.model.keras'
model_loader_name = 'json_load'
model_path = 'test/model_pol.json'
weights_path = 'test/trained_weights.hdf5'

# Model labels:
channel_order = N, E, Z
model_labels = {P: 0, S: 1, N: 2}
positive_labels = {P: 0, S: 1}

# Data settings:
allowed_channel_types[] = 'SH'
frequency = 100.

output_file = 'out.txt'
